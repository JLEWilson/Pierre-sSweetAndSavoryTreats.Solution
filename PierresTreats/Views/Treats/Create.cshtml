@model PierresTreats.Models.Treat

<h1>New treat details: </h1>

<h3>
  @using(Html.BeginForm())
  {
    @Html.LabelFor(model => model.Name)
    @Html.TextBoxFor(model => model.Name, 
    new {required="required"})

    <input type="submit" value="Add new treat" class="btn btn-lg btn-dark"/>
  }
</h3>
<div class="links">
  <h4>@Html.ActionLink("Cancel Creation", "Index")</h4>
</div>